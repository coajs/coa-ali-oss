{
  "name": "coa-ali-oss",
  "version": "1.0.1",
  "description": "third component for coa",
  "keywords": [
    "coajs",
    "coa",
    "ali",
    "oss"
  ],
  "license": "MIT",
  "author": "Aex",
  "homepage": "https://github.com/coajs/coa-ali-oss",
  "repository": {
    "type": "git",
    "url": "https://github.com/coajs/coa-ali-oss.git"
  },
  "scripts": {
    "dev": "tsc -w",
    "build": "rm -rf dist && tsc && cp package.json dist",
    "npm-publish": "yarn build && yarn version --patch && cp package.json README.md dist && cd dist && npm publish"
  },
  "dependencies": {
    "coa-error": "^1.1.3",
    "coa-helper": "^1.1.1",
    "coa-secure": "^1.1.2"
  },
  "devDependencies": {
    "@types/node": "^14.14.37",
    "typescript": "^4.2.4"
  }
}
